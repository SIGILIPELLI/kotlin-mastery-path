{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Kotlin Mastery Path","text":"<p>A structured, module-wise Kotlin training program that takes you from your first <code>fun main()</code> to production-shaped, master-level engineering \u2014 with runnable code snippets in every module and a hands-on project at the end of each level.</p>"},{"location":"#how-the-program-is-organized","title":"How the program is organized","text":"Level Focus Modules Level 1 \u00b7 Entry Language foundations 9 topics + 1 project Level 2 \u00b7 Intermediate Coroutines, testing, real apps 9 topics + 1 project Level 3 \u00b7 Advanced Ktor, databases, multiplatform 9 topics + 1 project Level 4 \u00b7 Master Production systems &amp; architecture 9 topics + 1 capstone"},{"location":"#how-to-use-this-site","title":"How to use this site","text":"<ul> <li>Work through each level in order \u2014 later modules assume earlier ones.</li> <li>Every topic page has runnable code snippets \u2014 copy them into a local <code>.kt</code>   file and run with <code>kotlinc</code>/<code>kotlin</code>, or paste into your IDE.</li> <li>Each level ends with a project that combines everything learned in that level.</li> <li>Use the search bar (top of the page) to jump straight to a topic.</li> </ul> <p>Start here \u2192 Level 1 \u00b7 Entry</p>"},{"location":"#more-from-the-mastery-path-series","title":"More from the Mastery Path series","text":"<p>Free, module-wise, entry-to-master training for other languages:</p> <ul> <li>Python Mastery Path</li> <li>Java Mastery Path</li> <li>JavaScript Mastery Path</li> <li>Shell Mastery Path</li> <li>C Mastery Path</li> <li>C++ Mastery Path</li> <li>Go Mastery Path</li> <li>SQL Mastery Path</li> <li>Rust Mastery Path</li> <li>TypeScript Mastery Path</li> <li>Ruby Mastery Path</li> <li>PHP Mastery Path</li> </ul>"},{"location":"privacy/","title":"Privacy Policy","text":"<p>Last updated: 2026-07-20</p> <p>This site (\"Kotlin Mastery Path\") uses Google AdSense to display advertising.</p>"},{"location":"privacy/#cookies-data-collection","title":"Cookies &amp; data collection","text":"<p>Google AdSense and its partners may use cookies, device identifiers, or similar technologies to serve ads based on your prior visits to this or other websites. Google's use of advertising cookies enables it and its partners to serve ads based on your visit to this site and/or other sites on the Internet.</p> <p>You can opt out of personalized advertising by visiting Google's Ads Settings or www.aboutads.info/choices.</p>"},{"location":"privacy/#what-this-site-does-not-do","title":"What this site does not do","text":"<p>This site does not require you to create an account, does not collect personal information through forms, and does not sell any data it might receive to third parties beyond the standard operation of the AdSense advertising network described above.</p>"},{"location":"privacy/#contact","title":"Contact","text":"<p>Questions about this policy can be directed via the GitHub repository for this site.</p>"},{"location":"level-1/","title":"Level 1 \u00b7 Entry Foundations","text":"<p>Goal: get comfortable reading and writing Kotlin, understand its core building blocks, and ship a small working program.</p>"},{"location":"level-1/#modules","title":"Modules","text":"<ol> <li>Setup &amp; First Program</li> <li>Variables &amp; Types</li> <li>Control Flow</li> <li>Functions</li> <li>Classes &amp; Objects Basics</li> <li>Null Safety Basics</li> <li>Collections</li> <li>Data Classes</li> <li>Extension Functions Intro</li> <li>Project \u2014 CLI To-Do App</li> </ol> <p>By the end of this level you'll be able to write programs that take input, make decisions, store and process data, work safely with nullable values, and organize code into classes, data classes, and extension functions \u2014 the idioms that make Kotlin feel different from Java from day one.</p>"},{"location":"level-1/01-setup/","title":"01 \u00b7 Setup &amp; First Program","text":"<p>Kotlin runs on the Java Virtual Machine (JVM), so you need a JDK installed before you can install Kotlin itself. This module gets your toolchain working and walks through your very first program, both as a compiled file and in the interactive REPL.</p>"},{"location":"level-1/01-setup/#installing-the-jdk","title":"Installing the JDK","text":"<p>Kotlin needs a JDK (Java Development Kit) underneath it \u2014 the Kotlin compiler produces JVM bytecode, and the <code>kotlin</code> command launches a JVM to run it.</p> <pre><code># macOS (Homebrew)\nbrew install openjdk@21\n\n# Ubuntu/Debian\nsudo apt install openjdk-21-jdk\n\n# Verify\njava -version\n# openjdk version \"21.0.x\" ...\n</code></pre>"},{"location":"level-1/01-setup/#installing-the-kotlin-compiler","title":"Installing the Kotlin compiler","text":"<pre><code># macOS (Homebrew)\nbrew install kotlin\n\n# SDKMAN (works on macOS/Linux, manages multiple versions cleanly)\ncurl -s \"https://get.sdkman.io\" | bash\nsdk install kotlin\n\n# Verify\nkotlinc -version\n# Kotlin version 2.0.x ...\n</code></pre> <p>Most people never touch <code>kotlinc</code> directly in real projects \u2014 Gradle or an IDE (IntelliJ IDEA, which has first-class Kotlin support since JetBrains builds both) drives the compiler for you. But knowing the raw command-line workflow first makes everything built on top of it easier to understand.</p>"},{"location":"level-1/01-setup/#your-first-program","title":"Your first program","text":"<p>Kotlin's entry point is a top-level function named <code>main</code> \u2014 no wrapping class required, unlike Java.</p> <pre><code>// Hello.kt\nfun main() {\n    println(\"Hello, Kotlin!\")\n}\n</code></pre> <p>Compile and run it in two steps:</p> <pre><code>kotlinc Hello.kt -include-runtime -d hello.jar\njava -jar hello.jar\n# Hello, Kotlin!\n</code></pre> <p>Or skip the explicit jar step and run the source file directly \u2014 convenient while learning:</p> <pre><code>kotlinc -script Hello.kts\n# or, for a plain .kt file with a main function:\nkotlin Hello.kt\n# Hello, Kotlin!\n</code></pre>"},{"location":"level-1/01-setup/#command-line-arguments","title":"Command-line arguments","text":"<p><code>main</code> can optionally take an <code>Array&lt;String&gt;</code> of arguments, matching what was passed on the command line.</p> <pre><code>// Greet.kt\nfun main(args: Array&lt;String&gt;) {\n    if (args.isEmpty()) {\n        println(\"Usage: Greet &lt;name&gt;\")\n        return\n    }\n    println(\"Hello, ${args[0]}!\")\n}\n</code></pre> <pre><code>kotlinc Greet.kt -include-runtime -d greet.jar\njava -jar greet.jar World\n# Hello, World!\n</code></pre>"},{"location":"level-1/01-setup/#the-kotlin-repl","title":"The Kotlin REPL","text":"<p>For quick experiments, the interactive shell evaluates expressions one at a time without a compile step.</p> <pre><code>kotlinc\n</code></pre> <pre><code>Welcome to Kotlin version 2.0.0\n&gt;&gt;&gt; val x = 10\n&gt;&gt;&gt; val y = 20\n&gt;&gt;&gt; println(x + y)\n30\n&gt;&gt;&gt; fun square(n: Int) = n * n\n&gt;&gt;&gt; square(5)\nres0: Int = 25\n&gt;&gt;&gt; :quit\n</code></pre> <p>The REPL is great for testing a one-off standard-library function or confirming how an expression evaluates before pasting it into a real file.</p>"},{"location":"level-1/01-setup/#println-vs-print","title":"<code>println</code> vs <code>print</code>","text":"<pre><code>fun main() {\n    print(\"No newline here... \")\n    print(\"still the same line.\\n\")\n    println(\"This adds a newline automatically.\")\n}\n</code></pre> <pre><code>No newline here... still the same line.\nThis adds a newline automatically.\n</code></pre>"},{"location":"level-1/01-setup/#cheat-sheet","title":"Cheat sheet","text":"Task Command Check JDK version <code>java -version</code> Check Kotlin compiler version <code>kotlinc -version</code> Compile to a runnable jar <code>kotlinc File.kt -include-runtime -d app.jar</code> Run a jar <code>java -jar app.jar</code> Run a <code>.kt</code> file directly <code>kotlin File.kt</code> Start the REPL <code>kotlinc</code> Exit the REPL <code>:quit</code>"},{"location":"level-1/01-setup/#exercise","title":"Exercise","text":"<p>Write a program <code>Info.kt</code> whose <code>main</code> function accepts two command-line arguments \u2014 a name and an age \u2014 and prints a sentence like <code>Alice is 30 years old.</code> If fewer than two arguments are given, print a usage message instead of crashing. Compile it to a jar and run it from the command line with <code>java -jar info.jar Alice 30</code>.</p>"},{"location":"level-1/02-variables-types/","title":"02 \u00b7 Variables &amp; Types","text":"<p>Kotlin is statically typed like Java, but with type inference doing most of the work and a hard distinction between things that can and can't be reassigned. Get comfortable with <code>val</code>/<code>var</code> and the basic types before anything else.</p>"},{"location":"level-1/02-variables-types/#val-vs-var","title":"<code>val</code> vs <code>var</code>","text":"<ul> <li><code>val</code> \u2014 a read-only reference. Once assigned, it can't be reassigned   (though if it points to a mutable object, that object's contents can   still change). Prefer <code>val</code> by default.</li> <li><code>var</code> \u2014 a mutable reference. Use it only when reassignment is actually   needed.</li> </ul> <pre><code>fun main() {\n    val name = \"Alice\"       // val -- cannot be reassigned\n    var age = 30             // var -- can be reassigned\n\n    age = 31                 // fine\n    // name = \"Bob\"          // compile error: val cannot be reassigned\n\n    println(\"$name is $age\")\n}\n</code></pre> <pre><code>Alice is 31\n</code></pre>"},{"location":"level-1/02-variables-types/#type-inference","title":"Type inference","text":"<p>Kotlin infers the type from the initializer, so explicit type annotations are usually optional \u2014 but you can always write them out.</p> <pre><code>fun main() {\n    val inferred = 42           // inferred as Int\n    val explicit: Int = 42      // same thing, spelled out\n    val price: Double = 19.99   // annotation required if you want Double from an integer literal\n\n    println(inferred)\n    println(explicit)\n    println(price)\n}\n</code></pre>"},{"location":"level-1/02-variables-types/#basic-types","title":"Basic types","text":"Type Example Notes <code>Int</code> <code>val x: Int = 42</code> 32-bit signed integer <code>Long</code> <code>val x: Long = 42L</code> 64-bit, note the <code>L</code> suffix <code>Double</code> <code>val x: Double = 3.14</code> 64-bit floating point (default for decimals) <code>Float</code> <code>val x: Float = 3.14f</code> 32-bit floating point, note the <code>f</code> suffix <code>Boolean</code> <code>val x: Boolean = true</code> <code>true</code>/<code>false</code> only <code>Char</code> <code>val x: Char = 'K'</code> single character, single quotes <code>String</code> <code>val x: String = \"Kotlin\"</code> double quotes <pre><code>fun main() {\n    val count: Int = 10\n    val total: Long = 10_000_000_000L\n    val pi: Double = 3.14159\n    val ratio: Float = 0.5f\n    val isReady: Boolean = true\n    val grade: Char = 'A'\n    val language: String = \"Kotlin\"\n\n    println(\"$count $total $pi $ratio $isReady $grade $language\")\n}\n</code></pre> <pre><code>10 10000000000 3.14159 0.5 true A Kotlin\n</code></pre> <p>Notice the underscore in <code>10_000_000_000L</code> \u2014 Kotlin lets you use <code>_</code> as a visual digit separator in numeric literals, purely for readability.</p>"},{"location":"level-1/02-variables-types/#everything-is-an-object","title":"Everything is an object","text":"<p>Unlike Java, Kotlin has no true \"primitives\" in the language itself \u2014 <code>Int</code>, <code>Double</code>, <code>Boolean</code>, and friends are all types with methods, and the compiler optimizes them down to JVM primitives where possible.</p> <pre><code>fun main() {\n    val x = 5\n    println(x.toString())        // \"5\" -- calling a method on an Int\n    println((-5).absoluteValue)  // 5 -- extension property from kotlin.math\n    println(x.coerceAtLeast(10)) // 10 -- another method available on Int\n}\n</code></pre> <pre><code>5\n5\n10\n</code></pre>"},{"location":"level-1/02-variables-types/#string-templates","title":"String templates","text":"<p>Instead of concatenation, Kotlin lets you embed expressions directly inside string literals with <code>$</code> (simple variable) or <code>${ }</code> (any expression).</p> <pre><code>fun main() {\n    val name = \"Alice\"\n    val age = 30\n\n    println(\"Name: $name\")                       // simple variable\n    println(\"Next year: ${age + 1}\")              // expression\n    println(\"Uppercase name: ${name.uppercase()}\") // method call\n}\n</code></pre> <pre><code>Name: Alice\nNext year: 31\nUppercase name: ALICE\n</code></pre>"},{"location":"level-1/02-variables-types/#type-conversion","title":"Type conversion","text":"<p>Kotlin does not auto-widen numeric types the way Java does \u2014 conversions are always explicit method calls.</p> <pre><code>fun main() {\n    val i: Int = 42\n    val l: Long = i.toLong()\n    val d: Double = i.toDouble()\n    val s: String = i.toString()\n    val parsed: Int = \"123\".toInt()\n\n    println(\"$l $d $s $parsed\")\n\n    // val bad: Long = i  // compile error -- no implicit widening in Kotlin\n}\n</code></pre> <pre><code>42 42.0 42 123\n</code></pre>"},{"location":"level-1/02-variables-types/#constants","title":"Constants","text":"<p><code>const val</code> declares a compile-time constant \u2014 must be a top-level or companion-object property, and must be a <code>String</code> or primitive type known at compile time.</p> <pre><code>const val MAX_USERS = 100\n\nfun main() {\n    println(\"Limit: $MAX_USERS\")\n}\n</code></pre>"},{"location":"level-1/02-variables-types/#cheat-sheet","title":"Cheat sheet","text":"Concept Syntax Read-only variable <code>val x = 10</code> Mutable variable <code>var x = 10</code> Explicit type <code>val x: Int = 10</code> Long literal <code>val x: Long = 10L</code> Float literal <code>val x: Float = 1.5f</code> String template <code>\"$name is ${age + 1}\"</code> Convert types <code>i.toLong()</code>, <code>i.toDouble()</code>, <code>\"42\".toInt()</code> Compile-time constant <code>const val NAME = value</code>"},{"location":"level-1/02-variables-types/#exercise","title":"Exercise","text":"<p>Write a program that declares a <code>var</code> holding a temperature in Celsius as a <code>Double</code>, converts it to Fahrenheit using the formula <code>f = c * 9 / 5 + 32</code>, and prints both values using a string template, e.g. <code>25.0\u00b0C is 77.0\u00b0F</code>. Then reassign the Celsius <code>var</code> to a new value and print the converted result again.</p>"},{"location":"level-1/03-control-flow/","title":"03 \u00b7 Control Flow","text":"<p>Kotlin's <code>if</code> and loops look familiar coming from Java, but <code>if</code> is an expression that can produce a value, and <code>when</code> \u2014 Kotlin's replacement for <code>switch</code> \u2014 is one of the language's signature idioms. Master <code>when</code> early; you'll use it constantly.</p>"},{"location":"level-1/03-control-flow/#if-as-an-expression","title":"<code>if</code> as an expression","text":"<p>In Kotlin, <code>if</code>/<code>else</code> can be used anywhere an expression is expected, replacing the ternary operator that Kotlin deliberately omits.</p> <pre><code>fun main() {\n    val age = 20\n\n    // Statement form -- familiar\n    if (age &gt;= 18) {\n        println(\"Adult\")\n    } else {\n        println(\"Minor\")\n    }\n\n    // Expression form -- assigns directly, no ternary operator needed\n    val status = if (age &gt;= 18) \"Adult\" else \"Minor\"\n    println(status)\n}\n</code></pre> <pre><code>Adult\nAdult\n</code></pre>"},{"location":"level-1/03-control-flow/#when-kotlins-switch-and-much-more","title":"<code>when</code> \u2014 Kotlin's <code>switch</code>, and much more","text":"<p><code>when</code> checks a value against multiple branches. Unlike Java's <code>switch</code>, it requires no <code>break</code> (no fallthrough by default), can match ranges and types, and can be used as an expression.</p> <pre><code>fun main() {\n    val day = 3\n\n    // Statement form\n    when (day) {\n        1 -&gt; println(\"Monday\")\n        2 -&gt; println(\"Tuesday\")\n        3 -&gt; println(\"Wednesday\")\n        else -&gt; println(\"Some other day\")\n    }\n\n    // Expression form -- must be exhaustive (else required unless all cases covered)\n    val dayName = when (day) {\n        1 -&gt; \"Monday\"\n        2 -&gt; \"Tuesday\"\n        3 -&gt; \"Wednesday\"\n        4, 5 -&gt; \"Almost the weekend\"   // multiple values, comma-separated\n        else -&gt; \"Unknown\"\n    }\n    println(dayName)\n}\n</code></pre> <pre><code>Wednesday\nWednesday\n</code></pre>"},{"location":"level-1/03-control-flow/#when-with-ranges-and-conditions","title":"<code>when</code> with ranges and conditions","text":"<pre><code>fun main() {\n    val score = 82\n\n    val grade = when (score) {\n        in 90..100 -&gt; \"A\"\n        in 80..89 -&gt; \"B\"\n        in 70..79 -&gt; \"C\"\n        else -&gt; \"F\"\n    }\n    println(grade)   // B\n\n    // No subject at all -- each branch is its own boolean condition\n    val temperature = 15\n    val description = when {\n        temperature &lt; 0 -&gt; \"Freezing\"\n        temperature in 0..15 -&gt; \"Cold\"\n        temperature in 16..25 -&gt; \"Mild\"\n        else -&gt; \"Hot\"\n    }\n    println(description)   // Cold\n}\n</code></pre>"},{"location":"level-1/03-control-flow/#when-with-types-is","title":"<code>when</code> with types (<code>is</code>)","text":"<pre><code>fun describe(value: Any): String = when (value) {\n    is Int -&gt; \"An integer: $value\"\n    is String -&gt; \"A string of length ${value.length}\"\n    is Boolean -&gt; \"A boolean: $value\"\n    else -&gt; \"Something else\"\n}\n\nfun main() {\n    println(describe(42))\n    println(describe(\"hello\"))\n    println(describe(true))\n}\n</code></pre> <pre><code>An integer: 42\nA string of length 5\nA boolean: true\n</code></pre> <p>Note that inside the <code>is String -&gt;</code> branch, <code>value</code> is automatically smart-cast to <code>String</code> \u2014 no explicit cast needed, unlike Java's older <code>instanceof</code> + cast pattern.</p>"},{"location":"level-1/03-control-flow/#for-loops","title":"<code>for</code> loops","text":"<p>Kotlin's <code>for</code> iterates over anything \"iterable\" \u2014 ranges, collections, strings \u2014 there is no classic C-style <code>for (int i = 0; ...)</code>.</p> <pre><code>fun main() {\n    // Range, inclusive of both ends\n    for (i in 1..5) {\n        print(\"$i \")\n    }\n    println()\n\n    // Range, exclusive of the end\n    for (i in 1 until 5) {\n        print(\"$i \")\n    }\n    println()\n\n    // Step and downTo\n    for (i in 10 downTo 0 step 2) {\n        print(\"$i \")\n    }\n    println()\n\n    // Over a collection\n    val fruits = listOf(\"apple\", \"banana\", \"cherry\")\n    for (fruit in fruits) {\n        print(\"$fruit \")\n    }\n    println()\n\n    // With index\n    for ((index, fruit) in fruits.withIndex()) {\n        println(\"$index: $fruit\")\n    }\n}\n</code></pre> <pre><code>1 2 3 4 5\n1 2 3 4\n10 8 6 4 2 0\napple banana cherry\n0: apple\n1: banana\n2: cherry\n</code></pre>"},{"location":"level-1/03-control-flow/#while-and-do-while","title":"<code>while</code> and <code>do-while</code>","text":"<pre><code>fun main() {\n    var count = 3\n    while (count &gt; 0) {\n        println(\"Countdown: $count\")\n        count--\n    }\n\n    var input = 0\n    do {\n        println(\"Runs at least once, input=$input\")\n        input++\n    } while (input &lt; 2)\n}\n</code></pre> <pre><code>Countdown: 3\nCountdown: 2\nCountdown: 1\nRuns at least once, input=0\nRuns at least once, input=1\n</code></pre>"},{"location":"level-1/03-control-flow/#break-continue-and-labels","title":"<code>break</code>, <code>continue</code>, and labels","text":"<pre><code>fun main() {\n    outer@ for (i in 1..3) {\n        for (j in 1..3) {\n            if (j == 2) continue@outer   // skip to next i, not just next j\n            println(\"i=$i j=$j\")\n        }\n    }\n}\n</code></pre> <pre><code>i=1 j=1\ni=2 j=1\ni=3 j=1\n</code></pre> <p>Labels (<code>outer@</code>) let <code>break</code>/<code>continue</code> target an outer loop directly \u2014 useful for nested loops without extra flag variables.</p>"},{"location":"level-1/03-control-flow/#cheat-sheet","title":"Cheat sheet","text":"Concept Syntax <code>if</code> as expression <code>val x = if (cond) a else b</code> <code>when</code> (switch replacement) <code>when (x) { 1 -&gt; ...; else -&gt; ... }</code> <code>when</code> with no subject <code>when { cond1 -&gt; ...; else -&gt; ... }</code> <code>when</code> with type check <code>when (x) { is String -&gt; ...; }</code> Inclusive range <code>1..5</code> Exclusive range <code>1 until 5</code> Reverse range <code>5 downTo 1</code> Step <code>1..10 step 2</code> Loop with index <code>for ((i, v) in list.withIndex())</code> Labeled continue <code>continue@outer</code>"},{"location":"level-1/03-control-flow/#exercise","title":"Exercise","text":"<p>Write a program that loops from 1 to 30 and, for each number, prints <code>\"Fizz\"</code> if divisible by 3, <code>\"Buzz\"</code> if divisible by 5, <code>\"FizzBuzz\"</code> if divisible by both, or the number itself otherwise \u2014 implement the branching with a single <code>when</code> expression (no chained <code>if</code>/<code>else if</code>). Then write a second version using ranges to categorize each number 1-30 as <code>\"low\"</code> (1-10), <code>\"mid\"</code> (11-20), or <code>\"high\"</code> (21-30) using <code>when</code> with <code>in</code> range checks.</p>"},{"location":"level-1/04-functions/","title":"04 \u00b7 Functions","text":"<p>Functions are top-level citizens in Kotlin \u2014 they don't need to live inside a class the way they must in Java. This module covers declaring functions, default and named arguments, and the compact single-expression syntax you'll see everywhere in idiomatic Kotlin code.</p>"},{"location":"level-1/04-functions/#basic-function-syntax","title":"Basic function syntax","text":"<pre><code>fun greet(name: String): String {\n    return \"Hello, $name!\"\n}\n\nfun main() {\n    println(greet(\"Alice\"))\n}\n</code></pre> <pre><code>Hello, Alice!\n</code></pre>"},{"location":"level-1/04-functions/#single-expression-functions","title":"Single-expression functions","text":"<p>When a function body is a single expression, you can drop the braces, <code>return</code>, and often the return-type annotation too (it's inferred).</p> <pre><code>// Full form\nfun add(a: Int, b: Int): Int {\n    return a + b\n}\n\n// Single-expression form -- equivalent, much shorter\nfun addShort(a: Int, b: Int): Int = a + b\n\n// Return type inferred from the expression\nfun square(n: Int) = n * n\n\nfun main() {\n    println(add(2, 3))\n    println(addShort(2, 3))\n    println(square(5))\n}\n</code></pre> <pre><code>5\n5\n25\n</code></pre>"},{"location":"level-1/04-functions/#default-arguments","title":"Default arguments","text":"<p>Parameters can have default values, so callers only need to supply what differs from the default \u2014 no method-overloading juggling required.</p> <pre><code>fun buildGreeting(name: String, greeting: String = \"Hello\", punctuation: String = \"!\"): String {\n    return \"$greeting, $name$punctuation\"\n}\n\nfun main() {\n    println(buildGreeting(\"Alice\"))                          // uses both defaults\n    println(buildGreeting(\"Bob\", \"Hi\"))                       // overrides greeting\n    println(buildGreeting(\"Carol\", punctuation = \"?\"))        // named arg, skips greeting\n}\n</code></pre> <pre><code>Hello, Alice!\nHi, Bob!\nHello, Carol?\n</code></pre>"},{"location":"level-1/04-functions/#named-arguments","title":"Named arguments","text":"<p>Any argument can be passed by name, in any order \u2014 especially useful with several parameters of the same type, where position alone is error-prone.</p> <pre><code>fun createUser(name: String, age: Int, isAdmin: Boolean = false) {\n    println(\"$name, age $age, admin=$isAdmin\")\n}\n\nfun main() {\n    createUser(name = \"Dana\", age = 28, isAdmin = true)\n    createUser(age = 35, name = \"Evan\")   // order doesn't matter with named args\n}\n</code></pre> <pre><code>Dana, age 28, admin=true\nEvan, age 35, admin=false\n</code></pre>"},{"location":"level-1/04-functions/#varargs","title":"Varargs","text":"<p><code>vararg</code> lets a function accept a variable number of arguments of the same type, collected into an array inside the function.</p> <pre><code>fun sum(vararg numbers: Int): Int {\n    var total = 0\n    for (n in numbers) total += n\n    return total\n}\n\nfun main() {\n    println(sum(1, 2, 3))\n    println(sum(1, 2, 3, 4, 5))\n\n    val values = intArrayOf(10, 20, 30)\n    println(sum(*values))   // spread operator -- unpacks an array into vararg\n}\n</code></pre> <pre><code>6\n15\n60\n</code></pre>"},{"location":"level-1/04-functions/#functions-as-first-class-values","title":"Functions as first-class values","text":"<p>A function can be assigned to a variable or passed as an argument using a function reference (<code>::name</code>) or a lambda \u2014 the basis for Kotlin's higher-order function style, covered in depth in Level 2.</p> <pre><code>fun double(n: Int) = n * 2\n\nfun main() {\n    val operation: (Int) -&gt; Int = ::double\n    println(operation(5))   // 10\n\n    val lambda: (Int) -&gt; Int = { n -&gt; n * 3 }\n    println(lambda(5))      // 15\n}\n</code></pre> <pre><code>10\n15\n</code></pre>"},{"location":"level-1/04-functions/#local-functions","title":"Local functions","text":"<p>Functions can be nested inside other functions, useful for small helpers that only make sense in one place.</p> <pre><code>fun processOrder(quantity: Int, price: Double): Double {\n    fun applyDiscount(amount: Double): Double =\n        if (quantity &gt; 10) amount * 0.9 else amount\n\n    val subtotal = quantity * price\n    return applyDiscount(subtotal)\n}\n\nfun main() {\n    println(processOrder(15, 2.0))   // 27.0 -- discount applied\n    println(processOrder(5, 2.0))    // 10.0 -- no discount\n}\n</code></pre> <pre><code>27.0\n10.0\n</code></pre>"},{"location":"level-1/04-functions/#cheat-sheet","title":"Cheat sheet","text":"Concept Syntax Basic function <code>fun name(param: Type): ReturnType { ... }</code> Single-expression function <code>fun name(param: Type) = expression</code> Default argument <code>fun f(x: Int = 10)</code> Named argument call <code>f(x = 5, y = 10)</code> Varargs <code>fun f(vararg nums: Int)</code> Spread operator <code>f(*array)</code> Function reference <code>::functionName</code> Function type <code>(Int) -&gt; Int</code>"},{"location":"level-1/04-functions/#exercise","title":"Exercise","text":"<p>Write a single-expression function <code>isPrime(n: Int): Boolean</code> that checks whether <code>n</code> is a prime number. Then write a function <code>describeNumbers</code> with a <code>vararg numbers: Int</code> parameter and a default <code>label: String = \"Numbers\"</code> parameter that prints the label followed by each number and whether it's prime, using <code>isPrime</code>. Call it once with a few numbers and once more with named <code>label</code> argument.</p>"},{"location":"level-1/05-classes-objects/","title":"05 \u00b7 Classes &amp; Objects Basics","text":"<p>Kotlin classes are more compact than Java's \u2014 constructor parameters and properties are often declared in one line. This module covers the essentials: defining a class, properties, constructors, and visibility.</p>"},{"location":"level-1/05-classes-objects/#a-basic-class","title":"A basic class","text":"<pre><code>class Person(val name: String, var age: Int) {\n    fun greet(): String {\n        return \"Hi, I'm $name and I'm $age years old.\"\n    }\n}\n\nfun main() {\n    val person = Person(\"Alice\", 30)\n    println(person.greet())\n    person.age = 31              // \"age\" is a var, so it can change\n    println(person.age)\n    // person.name = \"Bob\"       // compile error -- \"name\" is a val\n}\n</code></pre> <pre><code>Hi, I'm Alice and I'm 30 years old.\n31\n</code></pre> <p>This single line, <code>class Person(val name: String, var age: Int)</code>, replaces what would be a constructor plus fields plus getters/setters in Java \u2014 the primary constructor and its properties are declared together.</p>"},{"location":"level-1/05-classes-objects/#properties-with-custom-logic","title":"Properties with custom logic","text":"<p>Properties can have custom <code>get()</code>/<code>set()</code> bodies instead of just storing a value directly.</p> <pre><code>class Rectangle(val width: Double, val height: Double) {\n    val area: Double\n        get() = width * height   // computed every time it's accessed, not stored\n\n    val isSquare: Boolean\n        get() = width == height\n}\n\nfun main() {\n    val rect = Rectangle(4.0, 5.0)\n    println(rect.area)       // 20.0\n    println(rect.isSquare)   // false\n\n    val square = Rectangle(3.0, 3.0)\n    println(square.isSquare) // true\n}\n</code></pre> <pre><code>20.0\nfalse\ntrue\n</code></pre>"},{"location":"level-1/05-classes-objects/#secondary-constructors-and-init-blocks","title":"Secondary constructors and <code>init</code> blocks","text":"<p>The primary constructor can be supplemented with an <code>init</code> block (runs when the object is created) and secondary constructors for alternate ways to build an instance.</p> <pre><code>class Book(val title: String, val author: String, val pages: Int) {\n\n    init {\n        require(pages &gt; 0) { \"pages must be positive\" }\n        println(\"Created: $title\")\n    }\n\n    // Secondary constructor -- delegates to the primary with \"this(...)\"\n    constructor(title: String, author: String) : this(title, author, 0)\n}\n\nfun main() {\n    val book1 = Book(\"Kotlin in Action\", \"Dmitry Jemerov\", 360)\n    val book2 = Book(\"Untitled Draft\", \"Anonymous\")   // uses secondary constructor\n    println(\"${book1.title}: ${book1.pages} pages\")\n    println(\"${book2.title}: ${book2.pages} pages\")\n}\n</code></pre> <pre><code>Created: Kotlin in Action\nCreated: Untitled Draft\nKotlin in Action: 360 pages\nUntitled Draft: 0 pages\n</code></pre> <p><code>require(...)</code> throws an <code>IllegalArgumentException</code> with the given message if the condition is false \u2014 a common, concise way to validate constructor arguments.</p>"},{"location":"level-1/05-classes-objects/#member-functions-and-this","title":"Member functions and <code>this</code>","text":"<pre><code>class BankAccount(val owner: String, private var balance: Double) {\n\n    fun deposit(amount: Double) {\n        require(amount &gt; 0) { \"deposit amount must be positive\" }\n        this.balance += amount   // \"this\" is optional here, but clarifies intent\n    }\n\n    fun withdraw(amount: Double): Boolean {\n        if (amount &gt; balance) return false\n        balance -= amount\n        return true\n    }\n\n    fun getBalance(): Double = balance\n}\n\nfun main() {\n    val account = BankAccount(\"Alice\", 100.0)\n    account.deposit(50.0)\n    println(account.getBalance())        // 150.0\n    println(account.withdraw(200.0))     // false -- insufficient funds\n    println(account.withdraw(100.0))     // true\n    println(account.getBalance())        // 50.0\n}\n</code></pre> <pre><code>150.0\nfalse\ntrue\n50.0\n</code></pre>"},{"location":"level-1/05-classes-objects/#visibility-modifiers","title":"Visibility modifiers","text":"Modifier Visible from <code>public</code> (default) Everywhere <code>private</code> Only within the same class/file <code>protected</code> The class and its subclasses <code>internal</code> Anywhere in the same module <pre><code>class Counter {\n    private var count = 0   // hidden from outside code\n\n    fun increment() {\n        count++\n    }\n\n    fun current(): Int = count\n}\n\nfun main() {\n    val counter = Counter()\n    counter.increment()\n    counter.increment()\n    println(counter.current())   // 2\n    // counter.count += 1        // compile error -- \"count\" is private\n}\n</code></pre> <pre><code>2\n</code></pre>"},{"location":"level-1/05-classes-objects/#objects-object-for-singletons","title":"Objects: <code>object</code> for singletons","text":"<p>Kotlin has built-in singleton support via the <code>object</code> keyword \u2014 no manual \"private constructor + static instance\" boilerplate needed.</p> <pre><code>object AppConfig {\n    var appName = \"MyApp\"\n    var version = \"1.0.0\"\n\n    fun describe(): String = \"$appName v$version\"\n}\n\nfun main() {\n    println(AppConfig.describe())\n    AppConfig.version = \"1.1.0\"\n    println(AppConfig.describe())\n}\n</code></pre> <pre><code>MyApp v1.0.0\nMyApp v1.1.0\n</code></pre>"},{"location":"level-1/05-classes-objects/#cheat-sheet","title":"Cheat sheet","text":"Concept Syntax Class with constructor properties <code>class Person(val name: String, var age: Int)</code> Computed property <code>val area: Double get() = width * height</code> Init block <code>init { ... }</code> Secondary constructor <code>constructor(...) : this(...)</code> Validate arguments <code>require(condition) { \"message\" }</code> Private member <code>private var x</code> Singleton <code>object Name { ... }</code>"},{"location":"level-1/05-classes-objects/#exercise","title":"Exercise","text":"<p>Write a class <code>Circle(val radius: Double)</code> with a computed property <code>area</code> (<code>Math.PI * radius * radius</code>) and a computed property <code>circumference</code> (<code>2 * Math.PI * radius</code>). Add an <code>init</code> block that uses <code>require</code> to reject a non-positive radius. Then write a class <code>CircleCatalog</code> (as an <code>object</code> singleton) with a mutable list of circles, a function <code>addCircle(radius: Double)</code>, and a function <code>totalArea()</code> that sums the area of every circle added so far.</p>"},{"location":"level-1/06-null-safety/","title":"06 \u00b7 Null Safety Basics","text":"<p>Kotlin's type system distinguishes nullable from non-nullable types at compile time, which eliminates most <code>NullPointerException</code>s before the program even runs. This is one of Kotlin's most-loved features coming from Java \u2014 get comfortable with <code>?</code>, <code>!!</code>, <code>?:</code>, and safe calls now, since they're everywhere in idiomatic code.</p>"},{"location":"level-1/06-null-safety/#nullable-vs-non-nullable-types","title":"Nullable vs. non-nullable types","text":"<p>By default, a Kotlin type cannot hold <code>null</code>. To allow it, add <code>?</code> after the type.</p> <pre><code>fun main() {\n    var name: String = \"Alice\"\n    // name = null              // compile error -- String can't be null\n\n    var nickname: String? = \"Al\"   // String? -- nullable\n    nickname = null                 // fine\n    println(nickname)\n}\n</code></pre> <pre><code>null\n</code></pre> <p>The compiler tracks this distinction everywhere: a <code>String</code> parameter is guaranteed non-null by the time your function body runs, so you never need defensive null checks for it.</p>"},{"location":"level-1/06-null-safety/#the-safe-call-operator","title":"The safe call operator <code>?.</code>","text":"<p>Calling a method or property directly on a nullable type is a compile error. The safe call operator <code>?.</code> calls it only if the receiver isn't null, producing <code>null</code> otherwise.</p> <pre><code>fun main() {\n    val nickname: String? = null\n\n    // println(nickname.length)   // compile error -- nickname might be null\n    println(nickname?.length)     // null -- safe call short-circuits to null\n\n    val realNickname: String? = \"Al\"\n    println(realNickname?.length) // 2\n}\n</code></pre> <pre><code>null\n2\n</code></pre> <p>Safe calls chain naturally, short-circuiting to <code>null</code> at the first <code>null</code> link:</p> <pre><code>class Address(val city: String?)\nclass Person(val address: Address?)\n\nfun main() {\n    val person1: Person? = Person(Address(\"Boston\"))\n    val person2: Person? = Person(null)\n    val person3: Person? = null\n\n    println(person1?.address?.city)   // Boston\n    println(person2?.address?.city)   // null\n    println(person3?.address?.city)   // null\n}\n</code></pre> <pre><code>Boston\nnull\nnull\n</code></pre>"},{"location":"level-1/06-null-safety/#the-elvis-operator","title":"The Elvis operator <code>?:</code>","text":"<p><code>?:</code> supplies a default value when the expression on its left is <code>null</code> \u2014 named for how the operator looks like a smiley face when rotated.</p> <pre><code>fun main() {\n    val nickname: String? = null\n\n    val displayName = nickname ?: \"Anonymous\"\n    println(displayName)   // Anonymous\n\n    val length = nickname?.length ?: 0\n    println(length)         // 0 -- combines safe call + default in one line\n}\n</code></pre> <pre><code>Anonymous\n0\n</code></pre> <p><code>?:</code> can also be used to <code>return</code> or <code>throw</code> early, which is a very common Kotlin idiom for guard clauses:</p> <pre><code>fun printUpper(text: String?) {\n    val nonNull = text ?: return   // exit the function early if text is null\n    println(nonNull.uppercase())\n}\n\nfun main() {\n    printUpper(\"hello\")   // HELLO\n    printUpper(null)       // prints nothing, function returns immediately\n}\n</code></pre> <pre><code>HELLO\n</code></pre>"},{"location":"level-1/06-null-safety/#the-not-null-assertion","title":"The not-null assertion <code>!!</code>","text":"<p><code>!!</code> converts a nullable type to non-null by force \u2014 if the value actually is <code>null</code>, it throws a <code>NullPointerException</code> immediately. Use it sparingly, only when you are certain (from context the compiler can't see) that the value cannot be null.</p> <pre><code>fun main() {\n    val nickname: String? = \"Al\"\n    val length: Int = nickname!!.length   // \"I know this isn't null\"\n    println(length)\n\n    val missing: String? = null\n    // println(missing!!.length)   // throws NullPointerException at runtime\n}\n</code></pre> <pre><code>2\n</code></pre> <p>Avoid <code>!!</code> where possible</p> <p>Reaching for <code>!!</code> defeats the purpose of Kotlin's null safety \u2014 it's a promise to the compiler that can still blow up at runtime. Prefer <code>?.</code>, <code>?:</code>, or an explicit <code>if (x != null)</code> check instead. <code>!!</code> is best reserved for truly exceptional situations (e.g. a bug you want to crash loudly on) rather than routine null handling.</p>"},{"location":"level-1/06-null-safety/#smart-casts-with-explicit-null-checks","title":"Smart casts with explicit null checks","text":"<p>Once the compiler proves a nullable variable isn't <code>null</code> (e.g. inside an <code>if</code> block), it lets you use it as if it were non-null without <code>?.</code> or <code>!!</code>.</p> <pre><code>fun describe(nickname: String?) {\n    if (nickname != null) {\n        // Inside this block, nickname is smart-cast to String (non-null)\n        println(\"Nickname has ${nickname.length} characters\")\n    } else {\n        println(\"No nickname set\")\n    }\n}\n\nfun main() {\n    describe(\"Al\")\n    describe(null)\n}\n</code></pre> <pre><code>Nickname has 2 characters\nNo nickname set\n</code></pre>"},{"location":"level-1/06-null-safety/#safe-casts-with-as","title":"Safe casts with <code>as?</code>","text":"<p><code>as</code> throws a <code>ClassCastException</code> on a bad cast; <code>as?</code> returns <code>null</code> instead.</p> <pre><code>fun main() {\n    val obj: Any = \"hello\"\n\n    val asString: String? = obj as? String   // \"hello\" -- succeeds\n    val asInt: Int? = obj as? Int             // null -- fails safely, no exception\n\n    println(asString)\n    println(asInt)\n}\n</code></pre> <pre><code>hello\nnull\n</code></pre>"},{"location":"level-1/06-null-safety/#cheat-sheet","title":"Cheat sheet","text":"Operator Purpose Example <code>?</code> Declare a nullable type <code>var x: String? = null</code> <code>?.</code> Safe call \u2014 no-op if receiver is null <code>x?.length</code> <code>?:</code> Elvis \u2014 default if left side is null <code>x?.length ?: 0</code> <code>?:</code> + <code>return</code>/<code>throw</code> Guard clause <code>val v = x ?: return</code> <code>!!</code> Force non-null, throws if actually null <code>x!!.length</code> <code>as?</code> Safe cast \u2014 null instead of exception <code>obj as? String</code> smart cast Compiler narrows type after a null check <code>if (x != null) x.length</code>"},{"location":"level-1/06-null-safety/#exercise","title":"Exercise","text":"<p>Write a function <code>safeDivide(a: Int, b: Int): Int?</code> that returns <code>null</code> if <code>b</code> is zero, otherwise the integer division result. Then write a function <code>describeDivision(a: Int, b: Int): String</code> that calls <code>safeDivide</code> and uses the Elvis operator to produce <code>\"Result: N\"</code> on success or <code>\"Cannot divide by zero\"</code> on <code>null</code>. Test it with a few values including <code>b = 0</code>.</p>"},{"location":"level-1/07-collections/","title":"07 \u00b7 Collections","text":"<p>Kotlin's standard library ships with rich, well-designed collection types out of the box \u2014 <code>List</code>, <code>Set</code>, and <code>Map</code> \u2014 each with both read-only and mutable variants. This module covers creating and using them; the powerful functional operations (<code>map</code>, <code>filter</code>, <code>reduce</code>) get their own deep dive in Level 2.</p>"},{"location":"level-1/07-collections/#lists","title":"Lists","text":"<p>A <code>List</code> is an ordered collection that allows duplicates. <code>listOf(...)</code> creates a read-only list; <code>mutableListOf(...)</code> creates one you can modify.</p> <pre><code>fun main() {\n    val fruits: List&lt;String&gt; = listOf(\"apple\", \"banana\", \"cherry\")\n    println(fruits)          // [apple, banana, cherry]\n    println(fruits[0])       // apple\n    println(fruits.size)     // 3\n    // fruits.add(\"date\")    // compile error -- List has no add()\n\n    val mutableFruits: MutableList&lt;String&gt; = mutableListOf(\"apple\", \"banana\")\n    mutableFruits.add(\"cherry\")\n    mutableFruits.remove(\"apple\")\n    println(mutableFruits)   // [banana, cherry]\n}\n</code></pre> <pre><code>[apple, banana, cherry]\napple\n3\n[banana, cherry]\n</code></pre> <p>Note that <code>List</code> is read-only, not necessarily immutable \u2014 it simply doesn't expose mutating methods. It's a view; whether the underlying data can change elsewhere depends on what created it.</p>"},{"location":"level-1/07-collections/#common-list-operations","title":"Common list operations","text":"<pre><code>fun main() {\n    val numbers = listOf(5, 3, 8, 1, 9, 3)\n\n    println(numbers.first())          // 5\n    println(numbers.last())           // 3\n    println(numbers.contains(8))      // true\n    println(numbers.indexOf(8))       // 2\n    println(numbers.sorted())         // [1, 3, 3, 5, 8, 9]\n    println(numbers.reversed())       // [3, 9, 1, 8, 3, 5]\n    println(numbers.max())            // 9\n    println(numbers.min())            // 1\n    println(numbers.sum())            // 29\n    println(numbers.distinct())       // [5, 3, 8, 1, 9]\n}\n</code></pre> <pre><code>5\n3\ntrue\n2\n[1, 3, 3, 5, 8, 9]\n[3, 9, 1, 8, 3, 5]\n9\n1\n29\n[5, 3, 8, 1, 9]\n</code></pre>"},{"location":"level-1/07-collections/#sets","title":"Sets","text":"<p>A <code>Set</code> is an unordered collection with no duplicate elements.</p> <pre><code>fun main() {\n    val uniqueNumbers: Set&lt;Int&gt; = setOf(1, 2, 2, 3, 3, 3)\n    println(uniqueNumbers)          // [1, 2, 3] -- duplicates collapsed\n\n    val mutableSet: MutableSet&lt;String&gt; = mutableSetOf(\"a\", \"b\")\n    mutableSet.add(\"c\")\n    mutableSet.add(\"a\")             // no-op -- already present\n    println(mutableSet)             // [a, b, c]\n\n    val setA = setOf(1, 2, 3)\n    val setB = setOf(2, 3, 4)\n    println(setA.union(setB))         // [1, 2, 3, 4]\n    println(setA.intersect(setB))     // [2, 3]\n    println(setA.subtract(setB))      // [1]\n}\n</code></pre> <pre><code>[1, 2, 3]\n[a, b, c]\n[1, 2, 3, 4]\n[2, 3]\n[1]\n</code></pre>"},{"location":"level-1/07-collections/#maps","title":"Maps","text":"<p>A <code>Map</code> stores key-value pairs, with each key appearing at most once.</p> <pre><code>fun main() {\n    val ages: Map&lt;String, Int&gt; = mapOf(\"Alice\" to 30, \"Bob\" to 25)\n    println(ages)                 // {Alice=30, Bob=25}\n    println(ages[\"Alice\"])        // 30\n    println(ages[\"Unknown\"])      // null -- missing key returns null, not an exception\n    println(ages.getOrDefault(\"Unknown\", 0))   // 0\n    println(ages.containsKey(\"Bob\"))            // true\n\n    val mutableAges: MutableMap&lt;String, Int&gt; = mutableMapOf()\n    mutableAges[\"Carol\"] = 28\n    mutableAges[\"Dave\"] = 35\n    mutableAges[\"Carol\"] = 29     // overwrites the previous value\n    println(mutableAges)          // {Carol=29, Dave=35}\n}\n</code></pre> <pre><code>{Alice=30, Bob=25}\n30\nnull\n0\ntrue\n{Carol=29, Dave=35}\n</code></pre> <p>The <code>to</code> in <code>\"Alice\" to 30</code> creates a <code>Pair</code> \u2014 <code>mapOf</code> just takes a <code>vararg</code> of pairs.</p>"},{"location":"level-1/07-collections/#iterating-collections","title":"Iterating collections","text":"<pre><code>fun main() {\n    val fruits = listOf(\"apple\", \"banana\", \"cherry\")\n    for (fruit in fruits) {\n        println(fruit)\n    }\n\n    val ages = mapOf(\"Alice\" to 30, \"Bob\" to 25)\n    for ((name, age) in ages) {\n        println(\"$name is $age\")\n    }\n\n    // forEach as an alternative to a for loop\n    fruits.forEach { fruit -&gt; println(\"Fruit: $fruit\") }\n}\n</code></pre> <pre><code>apple\nbanana\ncherry\nAlice is 30\nBob is 25\nFruit: apple\nFruit: banana\nFruit: cherry\n</code></pre>"},{"location":"level-1/07-collections/#arrays-briefly","title":"Arrays (briefly)","text":"<p>Kotlin also has <code>Array&lt;T&gt;</code> (and primitive-specialized versions like <code>IntArray</code>), fixed-size and mutable in place. Lists are preferred for most everyday code; arrays show up mainly for performance-sensitive code or interop with Java APIs.</p> <pre><code>fun main() {\n    val numbers = arrayOf(1, 2, 3)\n    numbers[0] = 99\n    println(numbers.joinToString())   // 99, 2, 3\n\n    val ints = intArrayOf(1, 2, 3)    // specialized, avoids boxing\n    println(ints.sum())               // 6\n}\n</code></pre> <pre><code>99, 2, 3\n6\n</code></pre>"},{"location":"level-1/07-collections/#cheat-sheet","title":"Cheat sheet","text":"Task Syntax Read-only list <code>listOf(1, 2, 3)</code> Mutable list <code>mutableListOf(1, 2, 3)</code> Read-only set <code>setOf(1, 2, 3)</code> Read-only map <code>mapOf(\"a\" to 1, \"b\" to 2)</code> Mutable map <code>mutableMapOf&lt;String, Int&gt;()</code> Access by index <code>list[0]</code> Access map value <code>map[\"key\"]</code> (returns nullable) Add to mutable list <code>list.add(value)</code> Iterate map <code>for ((k, v) in map)</code> Combine key+value <code>\"key\" to value</code>"},{"location":"level-1/07-collections/#exercise","title":"Exercise","text":"<p>Write a program that builds a <code>MutableMap&lt;String, MutableList&lt;String&gt;&gt;</code> representing students grouped by grade (e.g. <code>\"A\" to mutableListOf(\"Alice\", \"Anna\")</code>). Add at least three grades with a couple of students each. Then print, for each grade in the map, the grade letter and the number of students in it, followed by their names \u2014 sorted alphabetically within each grade using <code>sorted()</code>.</p>"},{"location":"level-1/08-data-classes/","title":"08 \u00b7 Data Classes","text":"<p><code>data class</code> is one of Kotlin's most useful idioms: a single keyword that generates <code>equals()</code>, <code>hashCode()</code>, <code>toString()</code>, and a <code>copy()</code> function for a class whose main job is to hold data. This eliminates a huge amount of the boilerplate that plain Java classes require.</p>"},{"location":"level-1/08-data-classes/#declaring-a-data-class","title":"Declaring a data class","text":"<pre><code>data class Point(val x: Int, val y: Int)\n\nfun main() {\n    val p1 = Point(1, 2)\n    println(p1)                 // Point(x=1, y=2) -- toString() generated automatically\n}\n</code></pre> <pre><code>Point(x=1, y=2)\n</code></pre> <p>Compare that <code>println</code> output to a regular class, where you'd see something like <code>Point@1b6d3586</code> unless you wrote <code>toString()</code> yourself.</p>"},{"location":"level-1/08-data-classes/#generated-equals-and-hashcode","title":"Generated <code>equals()</code> and <code>hashCode()</code>","text":"<p>Data classes compare by value \u2014 two instances with the same property values are equal \u2014 unlike regular classes, which compare by reference.</p> <pre><code>data class Point(val x: Int, val y: Int)\n\nclass RegularPoint(val x: Int, val y: Int)\n\nfun main() {\n    val p1 = Point(1, 2)\n    val p2 = Point(1, 2)\n    println(p1 == p2)           // true -- structural equality\n\n    val r1 = RegularPoint(1, 2)\n    val r2 = RegularPoint(1, 2)\n    println(r1 == r2)           // false -- reference equality (different objects)\n\n    // hashCode() is consistent with equals(), so data classes work correctly as map keys\n    val counts = mutableMapOf&lt;Point, Int&gt;()\n    counts[Point(1, 2)] = 10\n    println(counts[Point(1, 2)])   // 10 -- found by value, not by reference\n}\n</code></pre> <pre><code>true\nfalse\n10\n</code></pre>"},{"location":"level-1/08-data-classes/#copy-modifying-one-field-at-a-time","title":"<code>copy()</code> \u2014 modifying one field at a time","text":"<p><code>copy()</code> creates a new instance with the same values, letting you override just the fields you want to change \u2014 essential for working with immutable (<code>val</code>-only) data classes.</p> <pre><code>data class User(val name: String, val email: String, val age: Int)\n\nfun main() {\n    val user = User(\"Alice\", \"alice@example.com\", 30)\n\n    val birthdayUser = user.copy(age = 31)   // everything else stays the same\n    println(user)            // User(name=Alice, email=alice@example.com, age=30)\n    println(birthdayUser)    // User(name=Alice, email=alice@example.com, age=31)\n}\n</code></pre> <pre><code>User(name=Alice, email=alice@example.com, age=30)\nUser(name=Alice, email=alice@example.com, age=31)\n</code></pre>"},{"location":"level-1/08-data-classes/#destructuring-declarations","title":"Destructuring declarations","text":"<p>Data classes automatically get <code>component1()</code>, <code>component2()</code>, etc. (matching constructor parameter order), which enables destructuring into separate variables in one line.</p> <pre><code>data class Point(val x: Int, val y: Int)\n\nfun main() {\n    val point = Point(3, 4)\n    val (x, y) = point   // destructuring -- calls component1()/component2() under the hood\n    println(\"x=$x, y=$y\")\n\n    // Very common with maps in a for loop, also destructuring-based\n    val ages = mapOf(\"Alice\" to 30, \"Bob\" to 25)\n    for ((name, age) in ages) {\n        println(\"$name: $age\")\n    }\n}\n</code></pre> <pre><code>x=3, y=4\nAlice: 30\nBob: 25\n</code></pre>"},{"location":"level-1/08-data-classes/#data-classes-with-default-values-and-validation","title":"Data classes with default values and validation","text":"<p>Data classes support everything regular classes do, including default parameter values and <code>init</code> block validation.</p> <pre><code>data class Product(\n    val name: String,\n    val price: Double,\n    val quantity: Int = 1\n) {\n    init {\n        require(price &gt;= 0) { \"price cannot be negative\" }\n        require(quantity &gt;= 0) { \"quantity cannot be negative\" }\n    }\n\n    val total: Double\n        get() = price * quantity\n}\n\nfun main() {\n    val item = Product(\"Widget\", 9.99, 3)\n    println(item)          // Product(name=Widget, price=9.99, quantity=3)\n    println(item.total)    // 29.97\n\n    val single = Product(\"Gadget\", 19.99)   // quantity defaults to 1\n    println(single)\n}\n</code></pre> <pre><code>Product(name=Widget, price=9.99, quantity=3)\n29.97\nProduct(name=Gadget, price=19.99, quantity=1)\n</code></pre>"},{"location":"level-1/08-data-classes/#when-to-use-a-data-class-vs-a-regular-class","title":"When to use a data class vs. a regular class","text":"Use a data class when... Use a regular class when... The type mainly holds data The type has significant behavior/identity beyond its data You want structural equality (<code>==</code> by value) You want reference equality or custom equality rules You'd benefit from <code>copy()</code> for updates Instances represent a mutable, evolving entity (e.g. a live connection) A readable <code>toString()</code> is helpful for logging/debugging You need full control over <code>toString()</code>/<code>equals()</code>"},{"location":"level-1/08-data-classes/#cheat-sheet","title":"Cheat sheet","text":"Feature Generated automatically for a <code>data class</code> <code>toString()</code> <code>ClassName(prop1=val1, prop2=val2)</code> <code>equals()</code> / <code>hashCode()</code> Structural (value-based) comparison <code>copy()</code> <code>instance.copy(prop = newValue)</code> <code>componentN()</code> Enables <code>val (a, b) = instance</code> destructuring"},{"location":"level-1/08-data-classes/#exercise","title":"Exercise","text":"<p>Define a data class <code>Movie(val title: String, val director: String, val year: Int, val rating: Double = 0.0)</code>. Create a list of several movies. Use <code>copy()</code> to create an updated version of one movie with a new <code>rating</code>. Then write a function that takes the list, destructures each movie in a loop, and prints <code>\"&lt;title&gt; (&lt;year&gt;) by &lt;director&gt; -- rating: &lt;rating&gt;\"</code> for each one.</p>"},{"location":"level-1/09-extension-functions/","title":"09 \u00b7 Extension Functions Intro","text":"<p>Extension functions let you add new functions to existing types \u2014 including types you don't own, like <code>String</code> or <code>Int</code> from the standard library \u2014 without inheritance or wrapper classes. This is one of the idioms that makes Kotlin code read so differently from Java, and the standard library itself (<code>map</code>, <code>filter</code>, and friends) is built almost entirely out of extensions.</p>"},{"location":"level-1/09-extension-functions/#declaring-an-extension-function","title":"Declaring an extension function","text":"<pre><code>fun String.shout(): String {\n    return this.uppercase() + \"!\"\n}\n\nfun main() {\n    val message = \"hello\"\n    println(message.shout())   // HELLO!\n}\n</code></pre> <pre><code>HELLO!\n</code></pre> <p>The type before the dot (<code>String</code>) is the receiver type. Inside the function body, <code>this</code> refers to the specific <code>String</code> instance the function was called on \u2014 same as inside a regular member function, even though <code>String</code> itself wasn't modified or subclassed.</p>"},{"location":"level-1/09-extension-functions/#extensions-on-your-own-types","title":"Extensions on your own types","text":"<pre><code>data class Rectangle(val width: Double, val height: Double)\n\nfun Rectangle.area(): Double = width * height\n\nfun Rectangle.isSquare(): Boolean = width == height\n\nfun main() {\n    val rect = Rectangle(4.0, 5.0)\n    println(rect.area())        // 20.0\n    println(rect.isSquare())    // false\n}\n</code></pre> <pre><code>20.0\nfalse\n</code></pre> <p>This is useful for adding capability to a <code>data class</code> you want to keep minimal and focused purely on holding data, with behavior layered on separately.</p>"},{"location":"level-1/09-extension-functions/#extension-functions-with-parameters","title":"Extension functions with parameters","text":"<p>Extension functions accept parameters exactly like regular functions.</p> <pre><code>fun Int.isDivisibleBy(divisor: Int): Boolean = this % divisor == 0\n\nfun main() {\n    println(10.isDivisibleBy(2))   // true\n    println(10.isDivisibleBy(3))   // false\n    println(15.isDivisibleBy(5))   // true\n}\n</code></pre> <pre><code>true\nfalse\ntrue\n</code></pre>"},{"location":"level-1/09-extension-functions/#extension-properties","title":"Extension properties","text":"<p>Extensions aren't limited to functions \u2014 you can add computed properties too (they must be computed, since there's no backing field to store state on a type you don't own).</p> <pre><code>val String.lastChar: Char\n    get() = this[this.length - 1]\n\nfun main() {\n    println(\"Kotlin\".lastChar)   // n\n}\n</code></pre> <pre><code>n\n</code></pre>"},{"location":"level-1/09-extension-functions/#extensions-on-nullable-types","title":"Extensions on nullable types","text":"<p>An extension can be declared on a nullable receiver type (<code>String?</code> instead of <code>String</code>), letting it safely handle the <code>null</code> case itself rather than forcing callers to write <code>?.</code> everywhere.</p> <pre><code>fun String?.orDefault(default: String): String {\n    return this ?: default\n}\n\nfun main() {\n    val name: String? = null\n    println(name.orDefault(\"Anonymous\"))   // Anonymous -- called directly, no ?. needed\n\n    val realName: String? = \"Alice\"\n    println(realName.orDefault(\"Anonymous\"))   // Alice\n}\n</code></pre> <pre><code>Anonymous\nAlice\n</code></pre>"},{"location":"level-1/09-extension-functions/#how-extension-functions-actually-resolve-and-their-limits","title":"How extension functions actually resolve (and their limits)","text":"<p>Extension functions are resolved statically, based on the declared type at the call site \u2014 they are not truly added to the class, just syntactic sugar for a regular function taking the receiver as its first argument. This means:</p> <ul> <li>They cannot access private members of the class they extend.</li> <li>They cannot be overridden polymorphically the way member functions can.</li> <li>If a member function and an extension function have the same signature,   the member function always wins.</li> </ul> <pre><code>fun main() {\n    val obj: Any = \"hello\"\n    // obj.shout()   // compile error even if shout() is defined for String --\n                      // \"obj\" is statically typed as Any, not String, at this call site\n}\n</code></pre>"},{"location":"level-1/09-extension-functions/#practical-example-a-small-utility-library","title":"Practical example: a small utility library","text":"<pre><code>fun List&lt;Int&gt;.average2(): Double {\n    if (this.isEmpty()) return 0.0\n    return this.sum().toDouble() / this.size\n}\n\nfun String.isValidEmail(): Boolean {\n    return this.contains(\"@\") &amp;&amp; this.substringAfter(\"@\").contains(\".\")\n}\n\nfun main() {\n    val numbers = listOf(4, 8, 15, 16, 23, 42)\n    println(numbers.average2())   // 18.0\n\n    println(\"alice@example.com\".isValidEmail())   // true\n    println(\"not-an-email\".isValidEmail())         // false\n}\n</code></pre> <pre><code>18.0\ntrue\nfalse\n</code></pre>"},{"location":"level-1/09-extension-functions/#cheat-sheet","title":"Cheat sheet","text":"Concept Syntax Extension function <code>fun ReceiverType.name(params): ReturnType { ... use this ... }</code> Extension property (computed only) <code>val ReceiverType.name: Type get() = ...</code> Extension on nullable type <code>fun String?.name(): Type { ... }</code> Resolution Static \u2014 based on declared type, member functions win over extensions"},{"location":"level-1/09-extension-functions/#exercise","title":"Exercise","text":"<p>Write an extension function <code>Int.factorial(): Long</code> that computes the factorial of a non-negative integer (use <code>require</code> to reject negative input). Write an extension property <code>List&lt;Int&gt;.median: Double</code> that returns the median of a non-empty list (sort it first; average the two middle values if the size is even). Test both with a few inputs.</p>"},{"location":"level-1/10-project-todo-app/","title":"10 \u00b7 Project \u2014 CLI To-Do App","text":"<p>A small end-to-end project combining everything from Level 1: data classes, collections, null safety, extension functions, and functions with default arguments. You'll build a command-line to-do list that persists tasks to a plain-text file between runs.</p>"},{"location":"level-1/10-project-todo-app/#what-youll-build","title":"What you'll build","text":"<p>A single-file Kotlin CLI to-do app that:</p> <ul> <li>Adds tasks with a description</li> <li>Lists all tasks, showing done/pending status</li> <li>Marks a task done by its number</li> <li>Removes a task by its number</li> <li>Persists everything to a plain-text file between runs</li> </ul>"},{"location":"level-1/10-project-todo-app/#project-layout","title":"Project layout","text":"<pre><code>todoapp/\n    Todo.kt\n</code></pre> <p>Everything lives in one file for this project \u2014 real multi-file Kotlin projects normally use Gradle (introduced in Level 2), but a single <code>.kt</code> file compiled directly with <code>kotlinc</code> is simplest while you're still in Level 1.</p>"},{"location":"level-1/10-project-todo-app/#todokt-the-data-class","title":"<code>Todo.kt</code> \u2014 the data class","text":"<pre><code>// Todo.kt\n\ndata class Task(val description: String, val isDone: Boolean = false) {\n\n    // Serialize to a single line for file storage: \"done|description\" or \"pending|description\"\n    fun toStorageLine(): String {\n        val status = if (isDone) \"done\" else \"pending\"\n        return \"$status|$description\"\n    }\n\n    companion object {\n        // Parse a stored line back into a Task; returns null if the line is malformed\n        fun fromStorageLine(line: String): Task? {\n            val parts = line.split(\"|\", limit = 2)\n            if (parts.size != 2) return null\n            val isDone = parts[0] == \"done\"\n            return Task(parts[1], isDone)\n        }\n    }\n}\n</code></pre>"},{"location":"level-1/10-project-todo-app/#extension-functions-for-display-formatting","title":"Extension functions for display formatting","text":"<pre><code>fun Task.displayLine(index: Int): String {\n    val marker = if (isDone) \"[x]\" else \"[ ]\"\n    return \"$index. $marker $description\"\n}\n\nfun List&lt;Task&gt;.countDone(): Int = this.count { it.isDone }\n</code></pre>"},{"location":"level-1/10-project-todo-app/#storage-loading-and-saving","title":"Storage \u2014 loading and saving","text":"<pre><code>import java.io.File\n\nclass TaskStorage(private val fileName: String) {\n\n    fun load(): MutableList&lt;Task&gt; {\n        val file = File(fileName)\n        if (!file.exists()) {\n            return mutableListOf()\n        }\n        return file.readLines()\n            .filter { it.isNotBlank() }\n            .mapNotNull { Task.fromStorageLine(it) }\n            .toMutableList()\n    }\n\n    fun save(tasks: List&lt;Task&gt;) {\n        val file = File(fileName)\n        file.writeText(tasks.joinToString(\"\\n\") { it.toStorageLine() })\n    }\n}\n</code></pre> <p><code>mapNotNull</code> transforms each line and drops any that came back <code>null</code> (i.e. malformed lines) in one step \u2014 a small preview of the collection operations covered fully in Level 2.</p>"},{"location":"level-1/10-project-todo-app/#the-cli-logic","title":"The CLI logic","text":"<pre><code>fun printUsage() {\n    println(\"Usage: todo [add &lt;description&gt; | list | done &lt;number&gt; | remove &lt;number&gt;]\")\n}\n\nfun addTask(tasks: MutableList&lt;Task&gt;, storage: TaskStorage, description: String?) {\n    val desc = description ?: run {\n        println(\"Error: add requires a description\")\n        return\n    }\n    tasks.add(Task(desc))\n    storage.save(tasks)\n    println(\"Added: $desc\")\n}\n\nfun listTasks(tasks: List&lt;Task&gt;) {\n    if (tasks.isEmpty()) {\n        println(\"No tasks yet.\")\n        return\n    }\n    tasks.forEachIndexed { index, task -&gt;\n        println(task.displayLine(index + 1))\n    }\n    println(\"${tasks.countDone()}/${tasks.size} done\")\n}\n\nfun markDone(tasks: MutableList&lt;Task&gt;, storage: TaskStorage, numberArg: String?) {\n    val number = numberArg?.toIntOrNull()\n    if (number == null || number &lt; 1 || number &gt; tasks.size) {\n        println(\"Error: done requires a valid task number\")\n        return\n    }\n    val index = number - 1\n    tasks[index] = tasks[index].copy(isDone = true)\n    storage.save(tasks)\n    println(\"Marked done: ${tasks[index].description}\")\n}\n\nfun removeTask(tasks: MutableList&lt;Task&gt;, storage: TaskStorage, numberArg: String?) {\n    val number = numberArg?.toIntOrNull()\n    if (number == null || number &lt; 1 || number &gt; tasks.size) {\n        println(\"Error: remove requires a valid task number\")\n        return\n    }\n    val removed = tasks.removeAt(number - 1)\n    storage.save(tasks)\n    println(\"Removed: ${removed.description}\")\n}\n\nfun main(args: Array&lt;String&gt;) {\n    val storage = TaskStorage(\"tasks.txt\")\n    val tasks = storage.load()\n\n    if (args.isEmpty()) {\n        printUsage()\n        return\n    }\n\n    when (args[0]) {\n        \"add\" -&gt; addTask(tasks, storage, args.drop(1).joinToString(\" \").ifBlank { null })\n        \"list\" -&gt; listTasks(tasks)\n        \"done\" -&gt; markDone(tasks, storage, args.getOrNull(1))\n        \"remove\" -&gt; removeTask(tasks, storage, args.getOrNull(1))\n        else -&gt; printUsage()\n    }\n}\n</code></pre> <p>Notice how much of Level 1 shows up here: <code>when</code> for command dispatch, the Elvis operator and <code>?:</code>/<code>run {}</code> for validating optional input, <code>toIntOrNull()</code> for safely parsing user input without a <code>try</code>/<code>catch</code>, <code>copy()</code> to update an immutable <code>Task</code> in place in the list, and an extension function (<code>displayLine</code>) to keep formatting logic out of the data class itself.</p>"},{"location":"level-1/10-project-todo-app/#running-it","title":"Running it","text":"<pre><code>kotlinc Todo.kt -include-runtime -d todo.jar\n\njava -jar todo.jar add \"Buy groceries\"\n# Added: Buy groceries\n\njava -jar todo.jar add \"Write Kotlin lesson\"\njava -jar todo.jar list\n# 1. [ ] Buy groceries\n# 2. [ ] Write Kotlin lesson\n# 0/2 done\n\njava -jar todo.jar done 1\n# Marked done: Buy groceries\n\njava -jar todo.jar list\n# 1. [x] Buy groceries\n# 2. [ ] Write Kotlin lesson\n# 1/2 done\n\njava -jar todo.jar remove 2\n# Removed: Write Kotlin lesson\n\njava -jar todo.jar list\n# 1. [x] Buy groceries\n# 1/1 done\n</code></pre> <p>Each run reloads <code>tasks.txt</code> from disk, so tasks persist across separate invocations of the program.</p>"},{"location":"level-1/10-project-todo-app/#stretch-goals","title":"Stretch goals","text":"<ul> <li>Add a <code>clear-done</code> command that removes every task where <code>isDone</code> is true.</li> <li>Add priority levels (<code>Task(description: String, isDone: Boolean = false,   priority: Int = 0)</code>) and sort <code>list</code> output by priority.</li> <li>Replace the plain-text storage format with JSON (you'll use a real JSON   library for this in Level 2, Module 7).</li> <li>Add a <code>search &lt;keyword&gt;</code> command using <code>filter</code> on the task list.</li> </ul> <p>Completing this project means you're ready for Level 2 \u00b7 Intermediate.</p>"},{"location":"level-2/","title":"Level 2 \u00b7 Intermediate Idiomatic Kotlin","text":"<p>Goal: write idiomatic, testable Kotlin \u2014 coroutines, functional collection operations, generics, and the tooling (Gradle, JUnit/Kotest, JSON) used in real projects.</p>"},{"location":"level-2/#modules","title":"Modules","text":"<ol> <li>OOP Deep Dive</li> <li>Lambdas &amp; Higher-Order Functions</li> <li>Coroutines Basics</li> <li>Null Safety Advanced</li> <li>Collections Deep Dive</li> <li>Testing with JUnit/Kotest</li> <li>Working with JSON</li> <li>Gradle Basics</li> <li>Generics</li> <li>Project \u2014 Weather CLI</li> </ol> <p>By the end of this level you'll write idiomatic, testable Kotlin: interfaces and sealed classes, functional collection pipelines, basic coroutines, and Gradle-managed projects with real dependencies.</p>"},{"location":"level-2/01-oop-deep-dive/","title":"01 \u00b7 OOP Deep Dive","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Interfaces, sealed classes, and inheritance idioms in Kotlin.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-2/02-lambdas-higher-order-functions/","title":"02 \u00b7 Lambdas &amp; Higher-Order Functions","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Lambda syntax, function types, and functions that take or return other functions.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-2/03-coroutines-basics/","title":"03 \u00b7 Coroutines Basics","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Suspend functions, <code>launch</code>/<code>async</code>, and coroutine builders and scopes.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-2/04-null-safety-advanced/","title":"04 \u00b7 Null Safety Advanced","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Platform types, <code>lateinit</code>, nullable generics, and Java interop nullability.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-2/05-collections-deep-dive/","title":"05 \u00b7 Collections Deep Dive","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. <code>map</code>/<code>filter</code>/<code>reduce</code>, sequences, and lazy evaluation.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-2/06-testing-junit-kotest/","title":"06 \u00b7 Testing with JUnit/Kotest","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. JUnit 5 basics, Kotest specs, assertions, and test fixtures.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-2/07-working-with-json/","title":"07 \u00b7 Working with JSON","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. <code>kotlinx.serialization</code> and data class (de)serialization.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-2/08-gradle-basics/","title":"08 \u00b7 Gradle Basics","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. <code>build.gradle.kts</code>, dependencies, tasks, and multi-module projects.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-2/09-generics/","title":"09 \u00b7 Generics","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Generic functions and classes, variance (<code>in</code>/<code>out</code>), and reified type parameters.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-2/10-project-weather-cli/","title":"10 \u00b7 Project \u2014 Weather CLI","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. A full CLI app combining an HTTP client, JSON parsing, and coroutines to fetch and display live weather data.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-3/","title":"Level 3 \u00b7 Advanced APIs &amp; architecture","text":"<p>Goal: build real backend services with Kotlin \u2014 Ktor APIs, database access, Flow-based coroutines, and multiplatform fundamentals.</p>"},{"location":"level-3/#modules","title":"Modules","text":"<ol> <li>Advanced Coroutines (Flow)</li> <li>Building APIs with Ktor</li> <li>Databases (Exposed/SQLite)</li> <li>Design Patterns in Kotlin</li> <li>Kotlin Multiplatform Basics</li> <li>Testing Advanced</li> <li>DSLs in Kotlin</li> <li>Android Basics</li> <li>Performance &amp; Profiling</li> <li>Project \u2014 REST API + Database Service</li> </ol> <p>By the end of this level you'll be able to design and build a tested, database-backed REST API with Ktor, understand Flow-based asynchronous streams, and know your way around common design patterns and DSL design in idiomatic Kotlin.</p>"},{"location":"level-3/01-advanced-coroutines-flow/","title":"01 \u00b7 Advanced Coroutines (Flow)","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. <code>Flow</code>, <code>StateFlow</code>/<code>SharedFlow</code>, and cold vs. hot streams.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-3/02-building-apis-ktor/","title":"02 \u00b7 Building APIs with Ktor","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Ktor server basics, routing, and request/response handling.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-3/03-databases-exposed-sqlite/","title":"03 \u00b7 Databases (Exposed/SQLite)","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. The Exposed DSL and DAO APIs, SQLite setup, and schema migrations.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-3/04-design-patterns/","title":"04 \u00b7 Design Patterns in Kotlin","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Singleton, Factory, Builder, and Strategy patterns implemented idiomatically in Kotlin.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-3/05-multiplatform-basics/","title":"05 \u00b7 Kotlin Multiplatform Basics","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Kotlin Multiplatform project structure, <code>expect</code>/<code>actual</code>, and shared modules.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-3/06-testing-advanced/","title":"06 \u00b7 Testing Advanced","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Mocking with MockK, integration tests, and test containers.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-3/07-dsls-in-kotlin/","title":"07 \u00b7 DSLs in Kotlin","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Type-safe builders and lambdas with receivers.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-3/08-android-basics/","title":"08 \u00b7 Android Basics","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. A brief overview of Android project structure, Activities, and Jetpack Compose.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-3/09-performance-profiling/","title":"09 \u00b7 Performance &amp; Profiling","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. JVM profiling tools and benchmarking with JMH.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-3/10-project-rest-api-db/","title":"10 \u00b7 Project \u2014 REST API + Database Service","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. A full REST API and database-backed service built with Ktor and Exposed.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-4/","title":"Level 4 \u00b7 Master Production systems","text":"<p>Goal: understand how production Kotlin systems are built, secured, deployed, and kept fast at scale \u2014 and ship a capstone project shaped like a real service.</p>"},{"location":"level-4/#modules","title":"Modules","text":"<ol> <li>Advanced Coroutines &amp; Structured Concurrency</li> <li>Production APIs with Ktor</li> <li>Multiplatform at Scale</li> <li>Security Best Practices</li> <li>Testing at Scale &amp; CI</li> <li>Deployment (Docker)</li> <li>Performance at Scale</li> <li>Java Interop Deep Dive</li> <li>Code Quality (detekt/ktlint)</li> <li>Capstone Project</li> </ol> <p>By the end of this level you'll understand how production-grade Kotlin services are architected, secured, tested, containerized, and deployed with CI/CD \u2014 and you'll have built one yourself.</p>"},{"location":"level-4/01-advanced-coroutines-structured-concurrency/","title":"01 \u00b7 Advanced Coroutines &amp; Structured Concurrency","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. <code>supervisorScope</code>, coroutine exception handling, and cancellation propagation.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-4/02-production-apis-ktor/","title":"02 \u00b7 Production APIs with Ktor","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Authentication, content negotiation, and production configuration for Ktor services.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-4/03-multiplatform-at-scale/","title":"03 \u00b7 Multiplatform at Scale","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Sharing business logic across iOS, Android, and backend targets in a real multiplatform codebase.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-4/04-security-best-practices/","title":"04 \u00b7 Security Best Practices","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Input validation, secrets management, and dependency auditing.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-4/05-testing-at-scale-ci/","title":"05 \u00b7 Testing at Scale &amp; CI","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Structuring large test suites and wiring them into GitHub Actions CI pipelines.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-4/06-deployment-docker/","title":"06 \u00b7 Deployment (Docker)","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Dockerizing a Ktor application with multi-stage builds.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-4/07-performance-at-scale/","title":"07 \u00b7 Performance at Scale","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Profiling, GC tuning, and load testing production services.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-4/08-java-interop-deep-dive/","title":"08 \u00b7 Java Interop Deep Dive","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Calling Java from Kotlin and vice versa, and the annotations that smooth over interop rough edges.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-4/09-code-quality/","title":"09 \u00b7 Code Quality (detekt/ktlint)","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Setting up detekt and ktlint, and enforcing static analysis in CI.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"},{"location":"level-4/10-capstone-project/","title":"10 \u00b7 Capstone Project","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. A production-shaped capstone project combining coroutines, Ktor, a database layer, testing, CI, and Docker deployment.</p> <p>Check the task ledger or ask your virtual employee for a status update on remaining content.</p>"}]}